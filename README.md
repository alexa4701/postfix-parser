# postfix-parser
C++ Programming languages project, simulates predictive parsing in a compiler.

Uses predictive parsing grammar to convert given infix notation expression to a postfix notation expression.
